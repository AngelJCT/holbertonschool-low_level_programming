#include <stdio.h>
/**
 * main - print all number from base 10 with putchar
 * Return: Always 0 (Success)
 */
int main(void)
{
	int x = 0;

	for (0 = 0; 0 <= 9; 0++)
	{
		putchar(x);
	}
	putchar('\n');
	return (0);
}
