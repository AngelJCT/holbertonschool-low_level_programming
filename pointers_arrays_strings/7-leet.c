#include "main.h"
/**
 *
 *
 *
 */
char *leet(char *ch)
{
	char lower[5] = {a, e, o, t, l};
	char upper[5] = {A, E, O, T, L};
	int num[5] = {4, 3, 0, 7, 1};

	for (lower = 'a'; lower <= 'z'; lower++)
	{
		lower = ch[num];
	}
	return (ch)
}
